# slope_deflection_framework — 課程路線圖

參照 [pyfem-plastic-hinge](https://github.com/zhixiu0223/pyfem-plastic-hinge) 的組織方式：
Case 編號 notebook、每個 Case 自成一個可獨立在 Colab 跑的教學單元、
ROADMAP.md 記錄驗證進度、每個完成的 Case 都要跟 anastruct 交叉驗證過
才算「過關」。

## 框架介面 (SlopeDeflectionProblem)

`sd_framework.py` 定義的抽象介面，每個 Case 的 `model_*.py` 都要實作
（必要方法）或視需要覆寫（可選方法）：

**必要方法**：`get_unknowns`, `describe`, `describe_dof`, `draw_geometry`,
`build_moment_equations`, `build_equilibrium_equations`, `draw_bmd`

（`describe` 只放結構/幾何/邊界條件描述；`describe_dof` 獨立成一個帶
警語的自由度說明區塊——這一步選錯或漏掉，後面所有方程式都會建立在
錯誤基礎上、整題全錯，值得跟結構描述分開、獨立強調。）

**可選方法**（有預設行為，不實作也不會出錯，求解器會優雅跳過）：
- `compute_reactions` — 步驟5 印支承反力，預設不計算
- `draw_sfd` — 步驟6 剪力圖，預設不畫，印提示跳過
- `draw_tension_side` — 在結構圖上直接標註每個桿端受拉/受壓側，用於
  `print_teaching_handout()` 的第4段，預設不畫，印提示跳過
- `teaching_breakdown` — 教學詳解+評分要點（Case-01 起新增）：
  回傳 list[dict]，每個 dict 是一個小題，包含
  `title/problem/concept/formula/substitution/answer/keywords/grading`，
  求解器會自動逐題印出「題目→概念解析→公式引用→帶入數據→參考答案→
  關鍵字→評分要點」，並加總配分。預設回傳空列表，不提供時印出
  「此模型尚未提供教學詳解，略過」而不報錯。**現在只在
  `print_teaching_handout()` 出現，`solve_and_report()` 的手算過程
  不再重複顯示。**

**共用工具函式**：`member_moment_curve`、`member_shear_curve`、
`member_offset_curve`（見下方「拉力彎矩圖」說明）。

**求解器的兩種輸出方式**：
- `solve_and_report()` — 完整步驟1~7，展開所有中間過程(公式引用、
  平衡方程式、解方程式)，適合看「怎麼算出來的」
- `print_teaching_handout()` — 只有「結構受力圖+自由度→教學詳解評分
  要點→剪力/彎矩圖→拉力側標註圖」四塊，沒有步驟編號、沒有中間符號
  方程式，適合直接複製當教學講義用

新增可選方法時務必記得：**model 的 class 一定要真的繼承
`SlopeDeflectionProblem`**（之前 Case-01/02 一度忘記繼承，只是靠鴨子
定型硬撐，新增 `teaching_breakdown` 時才炸出來——沒有繼承就拿不到
基底類別的預設實作）。

## 拉力彎矩圖 (Tension-Side Bending Moment Diagram) —— 畫圖規則

**這是 Case-03 抓到並修正的一個長期存在的錯誤**，記錄下來避免以後重犯：

- 之前一路以來，柱子的內力圖偏移方向都是憑感覺手寫的（每個Case各一套
  公式），沒有一個真正驗證過的物理規則
- Case-03 用 anastruct 放大截圖才發現：柱子其實也會「crossing」（沿
  桿長中途穿越桿件自身軸線），之前「柱不用轉換遠端符號」的判斷是錯的
- 用 anastruct 實際畫圖座標（不是端點數值，是整條線的真實x,y，精確到
  小數點後三位）反推出唯一正確、通用的規則：
  **偏移方向 = 沿桿件「近端→遠端」前進方向逆時針轉90度，正值往這個
  方向偏移**——這對應到結構分析/FEM裡標準的局部座標系慣例(局部y軸
  是局部x軸逆時針轉90度)，不是憑空湊出來的公式
- 用最單純無爭議的案例（懸臂梁，全長已知只有一種受拉側）驗證過：
  anastruct 的圖真的是畫在「受拉側」，我們的規則因為是照anastruct
  反推校準的，所以也是拉力彎矩圖——凸起的那一側就是實際受拉的那一側，
  不需要另外心算正負號對應哪一側
- 已做成共用工具 `member_offset_curve`，Case-03 的柱、梁都改用它，
  取代之前手寫的x/y公式
- **提醒**：原始的傾角變位法算出來的數值、正負號，從頭到尾都是對的
  （跟anastruct的raw值有時同號有時異號，這是兩套方法內部慣例不同、
  不代表誰錯——這件事本身也發現沒有一個簡單一致的規則，所以我們才
  堅持每個環節都要用剪力、座標這種可獨立驗證的物理量去核對，不能只
  看正負號猜）。**錯的只有「怎麼把數值畫成一條連續的線」這件事**，
  純粹是畫圖bug，不是計算bug

**已知影響範圍，尚未修正**：Case-04(側移剛架)、Case-05(二層對稱剛架)
的柱子畫圖用的是舊的錯誤邏輯，數值本身沒錯，需要時要用
`member_offset_curve` 重新修一次。

## 設計原則

1. **從最小的獨立位移自由度(Dk)開始**，每個 Case 只增加「一件新事情」
   （多一個未知數、多一種邊界條件、多一次側移、多一層樓...），
   絕不一次跳兩件事——這樣如果某個 Case 算錯，問題一定出在
   這次新加的那件事上，好抓。
2. **每個 Case 必做三件事**：
   - 用傾角變位法（我們的 Template Method + Strategy 框架）手算過程全部展開
   - 用 anastruct 建立完全獨立的 FEM 模型
   - **剪力交叉檢查**（不只比彎矩數值，也比剪力——這是這次抓到梁
     BC bug 的關页，剪力對不上通常代表畫圈/正負號有問題，比只比較
     彎矩數值更容易抓到「數值對、畫法錯」這種陷阱）
3. 每個 Case 完成後才進到下一個，不跳著做。
4. **教學詳解(`teaching_breakdown`)是可選的加分項**，不是每個 Case 的
   硬性要求——Case-01 已經示範過格式，之後哪個 Case 想額外補教學/
   評分內容，照 Case-01 的資料結構補上 `teaching_breakdown` 方法即可，
   沒補的 Case 不影響其他步驟正常運作。

## Validation Log

**表格說明**：`Ds`(靜不定度) 是贅力法的天然複雜度量，`Dk`(獨立位移自由度)
是傾角變位法的天然複雜度量——兩者是不同的東西，不要混用。目前這個 repo
只做傾角變位法，Case 的難度排序以 `Dk` 為主軸；`Ds` 先記錄下來備查，
等之後真的做贅力法比較時會用到。**沒有真正設計、驗證過的 Case，`Ds`欄
一律留白，不要用猜的填數字**——猜錯的數字比不填更誤導人。

| Case | 結構類型 | Ds (靜不定度) | Dk (獨立位移自由度，本課程主軸) | 新增的概念 | 狀態 |
|---|---|---|---|---|---|
| 01 | 一次靜不定梁 (propped cantilever, 固定+滾支承) | 1 | 1 (θ_B) | 邊界條件 M=0 取代節點平衡 | ✅ 完成，anastruct+剪力驗證通過 |
| 02 | 二次靜不定連續梁 (兩跨連續梁，三個支承) | 2 | 2 (θ_B, θ_C) | 中間支承的節點平衡；連續支承(共用θ)跟鉸接(各自獨立θ)的差別是這題最容易搞錯自由度的地方 | ✅ 完成，anastruct+剪力驗證通過 (四個端點全核對) |
| 03 | 無側移單跨剛架 (門型剛架，僅承受樑上均佈載重，柱不側移) | — (待設計時計算，不用猜) | 2 (θ_B, θ_C) | 樑柱剛架的節點平衡（柱剛度加入方程）、無側移(ψ=0)；第一次同時出現柱跟梁，抓到並修正了拉力彎矩圖、變形圖畫法的長期錯誤 | ✅ 完成，anastruct+OpenSeesPy+剪力+畫圖座標四重驗證通過 |
| 04 | 側移單跨剛架 (水平載重 P，即目前的「模型①」) | — | 3 (θ_B, θ_C, ψ=Δ/H) | 側移角 ψ、整體剪力平衡方程式(ΣFx=0)；跟Case-03唯一差別是拿掉無側移假設 | ✅ 完成，已搬入本專案並套用目前框架全部功能(FEM近端負遠端正、教學講義五張圖、拉力側、變形圖)，anastruct精確驗證(含B點側移量) |
| 05 | 二層對稱剛架 (即目前的「模型②」) | — | 4 (θ_B, θ_C, θ_D, θ_E，不預先對稱化簡) | 多層節點平衡；**這次刻意不做對稱化簡**(不預設θ_B=-θ_E這種鏡像關係)，讓4條方程式自己解出對稱結果，跟Case-03、04一路以來「不能用答案去猜題目」的原則一致——化簡成2個未知數是額外的進階技巧，但不是Case-05的必要步驟 | ✅ 完成，已搬入本專案並套用目前框架全部功能，anastruct精確驗證(六桿件十二端點) |
| 06 | 二層剛架 + 側移 (不對稱載重或有水平力) | — | 6 (θ_B,θ_C,θ_D,θ_E,ψ1,ψ2) | **修正規劃**：原本規劃4個未知數的前提是Case-05做對稱化簡(2個)後Case-06「拿掉化簡恢復4個」，但Case-05最後決定不做化簡(本來就是4個)。二層樓不對稱側移時，每層樓側移量不一定相同，嚴謹做法要各自設ψ1、ψ2(不能共用一個ψ，那是單層剛架Case-04的簡化)，所以是4轉角+2側移=6個未知數，多兩條樓層剪力平衡方程式(每層樓以上所有水平力要跟該層柱子剪力平衡) | ✅ 完成，anastruct精確驗證(十二桿端彎矩+兩層側移量) |
| 07 | 二跨不等跨剛架 | — | — | 不同跨度/剛度的樑柱組合 | ⬜ 待做 |

## ✅ 正負號慣例翻正：Case-03 已完成，Case-01/02 待補 (2026-08-19起)

**問題**：一路以來框架的 `describe_dof()` 文字都寫「順時針為正」，但實際數字
是**逆時針為正**——標籤跟數字對不上。已用三個獨立方法驗證：
1. 拿一個對稱無側移剛架例題(H=4,L=6,w=12)跟傳統教科書慣例(FEM近端負、
   遠端正)手算比對，我們的結果數值大小完全一致，但**正負號整組相反**
2. 用懸臂梁這種物理方向毫無疑問的參考案例校準：anastruct 的 `phi_z`
   正值 = 真正的物理順時針
3. 用 anastruct 實際算這個結構，`phi_z(B)` 是正值 = 物理上B真的是順時針轉，
   但我們框架算出 `theta_B` 是負值——確認我們的慣例其實是逆時針為正

**根本原因**：FEM 符號選反了。我們用 `FEM_近端=+wL^2/12, FEM_遠端=-wL^2/12`；
傳統教科書慣例是 `FEM_近端=-wL^2/12, FEM_遠端=+wL^2/12`。換過來之後，算出
來的每一個數值(θ、M)都跟傳統教科書例題逐項精確一致(連正負號都對上)。

**重要**：這不是計算錯誤——彎矩圖、剪力圖、拉力側、變形圖全部都經過
anastruct 反覆交叉驗證過，是對的、內部自洽的。錯的只是「這套自洽的
慣例」跟一般人手算傾角變位法時用的慣例不一樣，教學銜接不上，所以
決定整個翻正，不是因為算錯了才改。

**改動範圍**（全面改成 FEM近端負、遠端正）：
- 三個 Case 的 FEM 公式全部要反過來
- `draw_bmd`/`draw_sfd`/`draw_tension_side`/`draw_deformed_shape` 裡
  「遠端要negate」的規則整組換成「近端要negate」(因為M值全部反號，
  要維持同一個物理上正確的拉力側/變形圖，negate的對象要跟著換)
- `draw_deformed_shape` 額外要把邊界條件用的 `theta_B` 換成 `-theta_B`
  (跟M的negate對象換邊是同一件事的另一面)
- Case-01 教學詳解(`teaching_breakdown`)裡寫死的具體數值要重算
- `describe_dof()` 的文字說明本身不用改("順時針為正"這句話終於變成真的)

**驗證方法(唯一可靠的排錯方式)**：不能只看數值大小對不對，要用
anastruct 實際畫圖座標(`show_bending_moment()`/`show_displacement()`
背後的line資料，不是只看 node.Tz 這種原始陣列——那個陣列本身有跟
桿件定義方向綁定的怪癖，已經在Case-03踩過一次坑)逐點精確比對，
連座標、方向都對上才算真的驗證完成。抓變形圖bug時用「先只測梁、
再只測柱、確認規則一致後才接上完整結構」這個拆解方式，比一次測
整個結構好排查。

**目前狀態**：
- ✅ **Case-03 已完成**——`build_moment_equations`、`draw_sfd`、
  `draw_bmd`、`draw_tension_side`、`draw_deformed_shape` 全部已改成
  新慣例，並逐一用 anastruct 實際畫圖座標精確驗證過(含θ箭頭方向、
  拿掉跟箭頭重疊的文字符號，統一都畫成順時針、不依已解出的答案
  反著畫)。wget版notebook不用另外改，push後自動生效。
- ⬜ **Case-01、Case-02 尚未轉換**，還是舊慣例(FEM近端正、遠端負)。
  兩者本身沒有錯(仍然通過anastruct驗證)，只是跟Case-03用不同慣例，
  教學上不一致。之後要補的話，照上面Case-03的改動清單跟驗證方法
  照做一次。

## Colab 透過 GitHub 連結開啟時，sympy LaTeX 輸出不會顯示已存快照

**現象**：同一份 notebook，(a) 直接在 GitHub 網頁上看 `.ipynb`、或 (b)
把檔案上傳/本機開啟，都能正常看到存檔裡的 sympy 數學式；但 (c) 用
`colab.research.google.com/github/user/repo/blob/branch/path.ipynb`
這種連結開啟時，數學式一開始是空的，**每次**都要重新執行那個 cell 才會
出現(不是只有第一次)。已用專門的測試 repo
(`test-sympy-from-git2colab`) 隔離變因確認：不是 MathJax 冷啟動延遲
(那樣應該只有session第一次會這樣)，是 Colab 透過 GitHub 連結載入時，
不會正確讀出/渲染存在檔案裡的 `text/latex` 輸出快照，需要真的重新執行
才能產生「即時」輸出。

**影響**：README 裡「Open in Colab」徽章連結出去看到的第一印象，
sympy方程式那幾步會是空的，需要提醒使用者「打開後記得執行過一次」，
或考慮README同時提供GitHub網頁直接檢視的連結(存檔快照能看)。

## Case-04 搬入本專案並套用目前框架 (2026-08-20)

**背景**：ROADMAP 表格原本寫 Case-04「已完成」，但那是先前對話用**舊版框架**
(沒有 member_offset_curve、EI_numeric、教學講義五張圖、拉力側、變形圖、
FEM近端負遠端正這套慣例) 驗證過的結果——README 上也明確標示「待搬入本
專案重新包裝」。這次是**真的搬進來**，用目前框架整套重做一次，不是只改狀態文字。

**改動內容**：
- `model_sway_frame.py` 用目前的 `SlopeDeflectionProblem` 介面重寫，FEM
  查表用「近端負、遠端正」新慣例(本題FEM本身=0，慣例翻轉數值上不受影響，
  但寫法要跟其他Case統一)
- 新增第三個未知數 `psi`(=Δ/H)，`build_equilibrium_equations` 多一條
  整體剪力平衡方程式(ΣFx=0)，這是跟Case-03結構上唯一的差別
- `draw_sfd`/`draw_bmd`/`draw_tension_side`/`draw_deformed_shape` 全部
  套用跟Case-03一致的規則(近端negate、遠端直接；變形圖邊界條件用
  `-theta_B`)——**差別是這次不強制柱子的 u(H)=0**(Case-03無側移，柱頂
  水平位移=0是真實邊界條件；Case-04有側移，柱頂水平位移就是側移量Δ
  本身，不能再假設它是0，讓積分自然算出來的值當作Δ的驗證)
- `describe_dof()` 特別強調「這次不能假設無側移」這個跟Case-03的關鍵差異

**驗證**：跟 anastruct 精確比對彎矩(六個桿端值)、B點側移量(ψ×H算出的
理論值 vs anastruct實際ux)，全部吻合。

## Special Cases（特殊/進階案例，獨立編號，不排進 01~07 的階梯）

**這個區塊放什麼**：幾何跟現有 Case 完全不同、不是「Dk 遞增一個」能涵蓋的
題型——例如斜屋頂剛架、非正交桿件、考古題裡奇怪的混合支承組合。這些不是
教學階梯的下一步，是**測試框架本身泛用性**的題目，所以獨立編號
(`S-01`, `S-02`...)，不用擠進 01~07 的順序，加或不加都不影響主線進度。

**跟主線的關鍵差異**：01~07 都建立在同一個隱藏假設上——柱垂直、梁水平
剛性，所以側移角是單一純量 `ψ=Δ/H`（見 Case-04）。斜桿件的側移角要把
節點的 x,y 兩個位移分量投影到桿件自己的垂直方向，`sd_framework.py`
目前的一般式（`member_moment_curve` 那套 API）沒有處理這件事——這不是
寫一個新 `model_*.py` 就能解決的，是框架本身要先擴充。這也是之前討論
「通用宣告式輸入介面」被暫緩的那個方向會真正派上用場的地方。

| 編號 | 結構類型 | 需要框架先具備的能力 | 狀態 |
|---|---|---|---|
| S-01 | 斜屋頂剛架 (Gable/Pitched-roof Frame) | 斜桿件側移角(ψ)的一般化計算——節點位移拆 x,y 分量、投影到桿件局部垂直方向 | ⬜ 佔位，尚未開始，等framework補上斜桿件支援才能做 |

## Case-03 之後新增的框架能力（教學講義、拉力側、變形圖）

`sd_framework.py` 新增 `print_teaching_handout()`：只有「結構受力圖 →
教學詳解評分要點 → 剪力/彎矩圖(標拉力側提示) → 拉力側標註圖 → 變形圖」
五段，沒有步驟編號跟中間符號方程式，適合直接複製當教學講義。對應
新增可選方法 `draw_tension_side`、`draw_deformed_shape`，Case-01/02/03
都已實作。

**變形圖公式**：撓度用解析公式直接算（不要用 `sp.integrate` 符號積分——
手機瀏覽器版 Colab 對這種比較吃運算量的 cell 容易卡頓、需要多按一次才
畫得出來，換成純數值運算後解決了這個問題，速度快了15倍以上）。

**EI 參數化**：每個 Case 的建構子都加了 `EI_numeric`（預設15000.0），
只用在變形圖的撓度計算(彎矩、剪力、反力本身跟EI無關)。⚠️ anastruct
驗證那格的 `EI` 是獨立寫死的數字，換 model 的 `EI_numeric` 時記得同步
改，不然變形圖大小會對不起來(彎矩/剪力圖不受影響)。

**DOF 箭頭畫法（重要原則）**：設定未知數(θ_B, θ_C...)的箭頭方向時，
**全部統一假設同一個方向(順時針，標`(↻)`)**，不能依照已經算出來的正負
號反過來畫成有些順時針有些逆時針——那樣等於用答案去畫題目，失去「先
假設正方向、算完看正負號才知道實際方向」這個傾角變位法最基本的邏輯。
複雜結構在算之前根本不知道誰順誰逆。Case-01(單一未知數)、Case-02都已
經是這樣畫(兩個箭頭形狀一致)，Case-03原本畫成一順一逆(因為對稱結構
剛好解出一正一負，就順著答案去畫)，已修正成統一順時針。**之後
Case-04~07 畫DOF箭頭時要沿用這個原則。**

**Colab 表單 (`#@param`) 的教訓**：曾經在 wget notebook 加過
`{ run: "auto" }` 的互動輸入表單，結果造成「改參數時觸發自動重跑、
可能抓到打字打一半的中間值、還會連環觸發下一次重跑」，導致畫面
一直閃、算出錯誤結果。**已拿掉，換回純函數呼叫**——換參數就是直接
改程式碼那一行數字，不用互動表單。以後如果真的想要表單介面，至少
不能用 `run: "auto"`，要手動按執行鍵才觸發。

**環境自動偵測 (`IN_NOTEBOOK`)**：`sd_framework.py` 現在會自動偵測是
在 Jupyter/Colab 還是純終端機(`python3 xxx.py`)執行——Notebook環境
`display()`/`plt.show()`正常渲染；純終端機環境文字改用`print()`、
圖片自動存檔並印出檔名，不會變成一堆看不懂的
`<IPython.core.display.Markdown object>`。獨立驅動腳本
(`scripts/run_case03.py`)可以在 Colab/Linux/VSCode 三種環境直接跑，
支援命令列參數(`--H --L --w --EI`)跟互動輸入(`--interactive`)兩種
換參數方式。

## 重要教訓：外加節點彎矩會破壞「無側移」假設

在 Case-03 的結構上，如果只在 B 點單獨加一個外力矩（不是對稱地在
B、C都加），節點力矩平衡方程式本身（`M_BA+M_BC-M0=0`，順時針M0為正）
是對的，但**這個載重不再對稱，「無側移」的前提就不成立了**——實測
B、C兩點都出現明顯水平位移(anastruct的ux不再趨近於零)。這種情況要
改用含側移角Δ的三個未知數模型（跟Case-04同一套），不能直接套用
Case-03的二未知數模型。已用三方工具（傾角變位法手算、anastruct、
OpenSeesPy）交叉驗證過修正後的結果精確吻合。

**這是一個通用教訓，不只適用這一個案例**：換了載重型態(不只是換數字)
時，第一件事要重新檢查「這個載重還符不符合原本簡化模型的前提假設」
(對稱性、無側移這類假設)，不能只改方程式的一項就以為沒事。

**外加節點彎矩的規劃分類**：不佔用Case-01~07主線編號(那是自由度遞增
的階梯)，歸類成獨立的「V-01」載重變化練習題,還沒正式建成notebook。

**三方交叉驗證新增 OpenSeesPy**：除了 anastruct，現在也用 OpenSeesPy
(業界更廣泛驗證過的結構分析軟體)做第三方交叉比對，兩個獨立FEM工具
互相吻合、只有我們的結果對不上時，幾乎可以確定問題出在我們自己的
模型假設，不是工具的問題——這次就是靠這個方法定位出「少了側移自由度」
這個根本原因。

## 專案結構

```
slope_deflection_framework/
├── README.md
├── ROADMAP.md               <- 這個檔案
├── sd_framework.py          <- Template Method 骨架 (共用，不因Case而變)
├── samples/
│   ├── model_propped_cantilever.py
│   ├── model_two_span_beam.py
│   └── ...                  <- 之後新Case的 model_*.py 也放這裡
└── notebooks/
    ├── Case-01-propped-cantilever.ipynb
    ├── Case-01-propped-cantilever-wget.ipynb
    ├── Case-02-two-span-beam.ipynb
    ├── Case-03-no-sway-frame.ipynb        (待做)
    ├── Case-04-sway-frame.ipynb           (已完成)
    ├── Case-04-sway-frame-wget.ipynb      (已完成)
    ├── Case-05-two-story-frame.ipynb      (待做，先前對話已完成待搬入)
    └── ...
```
